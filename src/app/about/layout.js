export const metadata = {
    title: "About Us | Karobaar ERP",
    description: "Learn about the mission and vision behind Karobaar ERP, built by developers for the wholesale industry.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
